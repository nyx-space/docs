deploy:
  image: python:alpine
  script:
    - apk add npm
    - npm install -g firebase-tools
    - pip install mkdocs mkdocs-material
    - mkdocs build
    - firebase deploy --token $FIREBASE_TOKEN
  only:
    refs:
      - master